{
  "firestore": [
    {
      "database": "(default)",
      "rules": "firestore.rules"
    },
    {
      "database": "minionsbid",
      "rules": "firestore.rules"
    }
  ],
  "database": {
    "rules": "database.rules.json"
  }
}
