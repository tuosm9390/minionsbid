# Context Notes

## 2026-05-07

- The user asked to update project-specific instructions so realtime auction data remains fixed to the current code state.
- The relevant project-wide file is `AGENTS.md`; auction-specific instructions also live in `src/features/auction/AGENTS.md`, so both should carry the freeze rule.
- "Fixed to current code state" is interpreted as freezing the existing realtime auction contract: Firestore room hot-state fields, RTDB signal paths, event envelope shape, revision ordering, and convergence semantics should not be changed incidentally.
- This is a documentation/instruction change, not a runtime code change. Verification should be a focused diff review rather than running the full app test suite.
- Refactoring analysis found no evidence that the realtime auction data shape should be changed now. Current tests and rules already anchor `auction_revision`, `last_auction_event`, `AuctionEventEnvelope`, and `signals/{roomId}` paths.
- The main global-AGENTS-driven candidates are production console/error reporting policy, auction E2E timeout stability, and one test `any` in the schedule transaction mock. These can be addressed without changing realtime auction data contracts.
- Follow-up constraint from the user: do not touch currently used functionality. Console cleanup is therefore limited to review/no-op unless a log is clearly dead or test-only. Error reporting and debug-gated logs stay intact.
- Production console review found no production `console.log`. Remaining `console.info` calls are debug-gated, and `console.error`/`console.warn` calls are error reporting, so no active user-flow code was changed.
- `npm run test:e2e:auction` failed all 13 tests before reaching fixture room UI. The common server error was `The default Firebase app does not exist`, which points to the E2E runner starting a stale `.next` build instead of rebuilding with fixture env.
- To keep runtime functionality untouched, the fix is limited to `scripts/run_auction_e2e.js`: always run `next build` with fixture env before `next start`.
- After forcing rebuild, `next build` failed because the runner also forced `NODE_ENV=development`. The script no longer overrides `NODE_ENV`; Next can set the correct production build environment while fixture flags remain enabled.
- After direct fixture bid routing, auction E2E improved to 11/13 passing. The remaining failures are fixture-only issues: concurrent fixture bid requests are not serialized, and chat message revision bumps can mask the latest auction event latency marker. The fix stays inside `e2eAuctionFixture.ts`.
- Final `npm run test:e2e:auction` passed all 13 Playwright auction realtime tests after the fixture-only fixes.
- New issue: the user reports player draw / auction start UI appears to progress only while the auction window is focused.
- Initial code evidence points to `LotteryAnimation`: it calls `onFinished` only after Framer Motion `controls.start()` resolves. Browsers can throttle or pause animation frames for background tabs/windows, so the logical "draw finished" state can wait until focus returns.
- Keep the fix surgical: do not change Firestore/RTDB contracts or auction actions. Make the UI completion callback advance by elapsed wall-clock time, while preserving the existing animation when the window is focused.
- Implemented the fix in `LotteryAnimation` only. A guarded wall-clock fallback now marks the draw complete after the same configured duration even if Framer Motion's animation promise is delayed by focus/background throttling.
- Added `__tests__/LotteryAnimation.test.tsx`, mocking a never-resolving animation promise to verify the completion callback still fires once.
- Verification command passed: `npx vitest run __tests__/LotteryAnimation.test.tsx __tests__/useAuctionBoard.test.tsx src/features/auction/hooks/useAuctionControl.test.ts`.
- New issue: the user reports that bids from some leaders briefly show a 5-second timer reset and then rebound to the pre-bid timer value. This points to disagreement between optimistic/RTDB state and Firestore canonical snapshot.
- Existing contract says bids only extend when remaining time is below 5 seconds. The user is explicitly asking to change the behavior so every accepted leader bid resets the auction timer to 5 seconds.
- Because this changes timer behavior, update code, Firestore rules, fixture, tests, and `doc/AUCTION_REALTIME_CONTRACT.md` together without changing the event envelope shape or Firestore/RTDB paths.
- Implemented reset-to-5 for every successful bid in `placeBidDirect`, Server Action `placeBid`, and E2E fixture `placeFixtureBid`.
- Updated Firestore rules so direct leader bids must write `timer_ends_at` near request time + 5s, instead of allowing unchanged timers.
- Updated `useAuctionRealtime` convergence so a newer Firestore revision can shorten the local timer. This is required because a valid bid reset can move a 10s start timer down to 5s.
- Verification passed: `npx vitest run __tests__/auctionActions.test.ts __tests__/useBiddingControl.test.tsx __tests__/useAuctionRealtime.test.tsx`, `npm run test:e2e:auction`, and `git diff --check`.
- E2E passed all 13 auction tests. Fixture mode still logs fire-and-forget `broadcastBidEvent` Firebase Admin warnings after completion, but the tested flows pass and converge.
- Follow-up correction: the desired bid timer policy is not every-bid reset. A successful bid should reset the timer to 5 seconds only when the remaining canonical timer is under 5 seconds. If at least 5 seconds remain, the canonical `timer_ends_at` should stay unchanged while `active_bid` and revision still advance.
- Keep the previous convergence fix that lets newer Firestore snapshots apply shorter timers, because threshold extension can still produce shorter values in stale local states.
- Verification passed after the correction: `npm run test -- __tests__/auctionActions.test.ts __tests__/useBiddingControl.test.tsx __tests__/useAuctionRealtime.test.tsx`. `git diff --check` reported only existing line-ending normalization warnings.
- New CSP issue: Firebase RTDB long-polling attempted to load a script from `https://s-gke-apse1-nssi2-12.asia-southeast1.firebasedatabase.app`, while `script-src-elem` only allowed the configured database origin. RTDB can use sharded `*.firebasedatabase.app` hosts for long-polling, so script sources need the same wildcard host allowance already present in `connect-src`.
- Implemented the CSP fix in `src/proxy.ts` by allowing `https://*.firebasedatabase.app` and `https://*.firebaseio.com` in script sources. Added `__tests__/proxy.test.ts` to lock the RTDB sharded long-polling allowance. Verification passed: `npm run test -- __tests__/proxy.test.ts` after rerunning with elevated permissions because the first sandboxed run failed before tests with `spawn EPERM`.
- New two-leader timer rebound issue: direct bid success writes the new timer locally but does not advance `auctionEventRevision`. A delayed RTDB event or Firestore room snapshot from the previous bid can then look newer than local state and overwrite the 5-second optimistic/direct timer with the previous bid's already-counted-down timer, e.g. 3 seconds.
- Fixed the rebound by setting `auctionEventRevision` from `placeBidDirect`'s returned revision after direct bid success. This prevents stale prior bid RTDB/Firestore events from applying after the successful newer direct bid. Verification passed: `npm run test -- __tests__/useBiddingControl.test.tsx __tests__/useAuctionRealtime.test.tsx` after rerunning with elevated permissions because the first sandboxed run failed before tests with `spawn EPERM`.
- The two-leader issue still reproduces when the bidder's UI sees under 5 seconds and optimistically resets, but the Firestore direct transaction decides not to extend because it compares the canonical timer against that bidder browser's clock. This can happen with clock skew or delayed event delivery. The fix should pass the local under-5s intent into the direct transaction so the write path uses the same timer update behavior for non-first bids.
- Implemented forced reset intent plumbing from `useBiddingControl` to `placeBidDirect` and the E2E fixture command. Verification passed: `npm run test -- __tests__/useBiddingControl.test.tsx __tests__/auctionActions.test.ts __tests__/useAuctionRealtime.test.tsx` after sandbox `spawn EPERM` required elevated rerun, and `npm run build`.
- Firestore rules now allow either preserving the timer or extending it to a near-now 2-8s value, but the extension path also requires `after.timer_ends_at >= before.timer_ends_at` so a leader cannot shorten a healthy longer timer through a crafted direct write.
- New observation from the user: the timer visibly returns to about 3 seconds when the chat list updates after a non-first bidder bids. `useAuctionRealtime` chat listeners only call `setMessages` or `appendMessage`, so chat itself is likely a timing signal rather than the direct timer mutator.
- The relevant risk is convergence around `broadcastBidEvent`: direct bid success locally sets `auctionEventRevision`, then the room snapshot and RTDB/message fanout can arrive in different orders. A regression should assert that message fanout and same-revision bid events do not shorten an already-extended direct-bid timer.
- The concrete gap is the Server Action fallback path. If `placeBidDirect` fails and `placeBid` succeeds, the hook applied the returned timer but did not know the new revision, leaving local `auctionEventRevision` stale until RTDB/Firestore caught up. During that window, chat/system-message fanout can coincide with older realtime events applying and visually reverting the timer.
- The fix is to return the `BID_PLACED` revision from `placeBid`, propagate fixture direct-bid revisions, and make `useBiddingControl` record the revision after Server Action fallback success as it already does for direct success.
- New ultrawork pass: enumerate all paths that can make the timer appear shorter. Candidate classes are direct bid result, Server Action fallback result, RTDB `auctionEvent`, RTDB `auctionEvents` history, room snapshot state, room snapshot fallback event, messages snapshot, latestMessage signal, bids snapshot, players snapshot, presence pause/resume, award recovery, and `CenterTimer` local render math.
- Important suspicion beyond direct `timerEndsAt` writes: `useAuctionRealtime` already avoids applying stale room snapshot timer state, but it still runs side effects such as expiry wake-up scheduling from the raw snapshot timer even when that snapshot is older than the locally applied auction revision.
- Confirmed failing paths with tests: a direct bid room snapshot can apply `timerEndsAt`/`liveBid` before `broadcastBidEvent` has written `last_auction_event`, but the hook did not advance `auctionEventRevision` from that canonical bid snapshot. A same-revision stale RTDB event could then still apply and shorten the timer. Also, older room snapshots could still schedule expiry recovery side effects after a newer revision was already local.
- Fixed `useAuctionRealtime` without changing event shape or paths: canonical room snapshots containing active bid state now advance `auctionEventRevision` if no newer event has done so, and stale room snapshots no longer schedule expiry wake-up/recovery side effects. Added coverage that latestMessage/messages/bids/players fanout does not mutate `timerEndsAt`.
- `doc/AUCTION_REALTIME_CONTRACT.md` had an outdated Timer Rules sentence saying every bid resets the timer. Updated it to the current under-5-seconds extension policy.
- Follow-up issue still reproduces in the user's live/manual two-leader test. Need separate deployed-environment/config suspicion from local code behavior, because local tests and E2E covered the stale chat fanout path.
- `firebase.json` is a Firebase CLI deployment manifest, not runtime app config. In this project Vercel serves the Next app, while Firebase CLI uses `firebase.json` to deploy Firestore and Realtime Database rules.
- Local `.env.local` includes `FIRESTORE_DATABASE_ID=minionsbid` and `NEXT_PUBLIC_FIRESTORE_DATABASE_ID=minionsbid`, so local client/server code targets the named Firestore database. Production must have the same two env vars or it will fall back to `(default)`.
- Current working tree had removed the `(default)` Firestore rules entry from `firebase.json`, even though README and progress notes say rules should be configured for both `(default)` and `minionsbid`.
- New experiment request: comment out existing timer update behavior and make the client accumulate 5 seconds when an RTDB `BID_PLACED` value is delivered.
- Interpretation: keep Firestore writes and bid validation unchanged for now, but change realtime client application so `BID_PLACED` does not trust event/room `timerEndsAt` and instead computes `max(currentTimer, now) + 5000ms`.
- Implemented the experiment in client realtime convergence only. `BID_PLACED` now computes `timerEndsAt = max(current timer, Date.now()) + 5s`; direct/server-action bid responses no longer pre-apply timer/revision; bid Firestore snapshots no longer overwrite the timer or pre-advance revision.
- Focused verification passed: `npm run test -- __tests__/useAuctionRealtime.test.tsx __tests__/useBiddingControl.test.tsx __tests__/auctionActions.test.ts`, `npm run build`, and `npm run test:e2e:auction`. E2E fixture still logs known `broadcastBidEvent failed` Admin init warnings after passing.
- New issue: during player lottery, chat/message updates appear to prevent lottery completion. Message listeners only update message state, but `useAuctionBoard` reset `lotteryDone` whenever the `lotteryPlayer` object reference changed.
- Confirmed with a failing `useAuctionBoard` regression: after `lotteryDone=true`, updating chat state and passing a cloned same-id lottery player reset completion to false. Fixed by resetting lottery completion only when `lotteryPlayer.id` changes.
- Follow-up: the remaining lottery/chat coupling is inside `LotteryAnimation`. `AuctionBoard` passes an inline `onFinished` callback, and `LotteryAnimation` restarts its effect whenever that callback identity changes. Any chat-triggered parent rerender can reset the animation timer before completion or replay the finished animation after completion.
- Fixed `LotteryAnimation` by storing the latest `onFinished` in a ref and keying the animation lifecycle on `targetPlayer.id`, not parent callback identity or cloned same-id player objects. Added tests for rerender before completion and rerender after completion.
- New issue: timer only refreshes when the first bidder presses the bid button; subsequent bidders' presses do not update the timer for other users.
- Root cause: in `broadcastBidEvent`, the RTDB `publishAuctionEvent` call was gated behind an `await` on the Firestore `last_auction_event` update. If that Firestore write was slow or failed, the entire catch block fired without ever publishing to RTDB, so no `BID_PLACED` event reached other clients.
- Fix: restructured `broadcastBidEvent` so RTDB is published FIRST (in its own try/catch), then the Firestore write and chat message are fire-and-forget. Timer accumulation via RTDB now fires for every bidder independently of chat data.
- Verification: `npm test -- __tests__/useAuctionRealtime.test.tsx __tests__/useBiddingControl.test.tsx` — 32/32 passed.
